MCP Enterprise Demo

This repository contains multiple minimal MCP FastAPI services and an orchestrator.

Each MCP is self-contained and exposes HTTP endpoints. The orchestrator routes requests to MCPs and logs to the Audit MCP.
